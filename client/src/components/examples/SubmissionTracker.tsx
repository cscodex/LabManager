import { SubmissionTracker } from '../SubmissionTracker';

export default function SubmissionTrackerExample() {
  return (
    <div className="p-6 max-w-6xl mx-auto">
      <SubmissionTracker />
    </div>
  );
}